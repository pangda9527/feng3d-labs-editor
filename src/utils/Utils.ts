namespace feng3d.editor
{
    export var utils: Utils;
    export class Utils
    {
        /**
         * 获取所有类
         */
        getAllClasss(root = window, rootpath: "", depth = 5)
        {
            Object.keys(root).forEach((key) =>
            {
                
            });
        }
    }

    utils = new Utils();
}